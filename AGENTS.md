# rtctrl-platform 开发协作

本仓库直接实现、排错、重构、构建和代码审查使用项目 skill：
[rtctrl-dev](.agents/skills/rtctrl-dev/SKILL.md)。纯问答按需读取相关部分。
用户表达“教我”“我想自己写”“给提示”“带我读源码”等学习意图时，优先使用
[rtctrl-mentor](.agents/skills/rtctrl-mentor/SKILL.md)，默认只读并让用户亲手实现；
rtctrl-dev 此时仅提供工程约束，不自动接管练习。用户明确要求代写或改文件时按最新请求切换。
用户当前明确要求优先于本文件和 skill 的默认约定。

- 默认中文交流；从当前需求、相关源码和实际构建关系理解任务，保留已有未提交修改。
- 有独立且有价值的分析或审查任务时，请使用子 agent；简单修改直接完成。
  分工和工具不可用时的回退见 skill；主 agent 负责最终验证与整合。
- 开发中出现明确的长期偏好或已验证的重复问题时，按 skill 的持续完善流程更新项目知识。
  没有新证据时不制造规则；不把单次需求、推测或临时错误固化成全局约束。

可显式调用：`$rtctrl-dev 帮我完善 RKNN backend，并验证实际参与构建的目标。`

教学示例：`$rtctrl-mentor 我想自己写 RKNN 资源管理，先讲原理和取舍，再给第一步提示。`

## 跨任务项目记忆

开始本仓库的新任务时，先读取 [项目记忆](.agents/PROJECT_MEMORY.md)，再按其中链接读取相关证据。
这些文件是跨任务的交接入口，不假设新任务能看到旧聊天。最新用户要求优先；运行状态、PID、地址和测量条件必须重新核实。
完成有实质变化的任务后，更新这份记忆的当前状态及证据链接；长期经验继续维护在原有 lessons.md，避免重复复制。
只记录已验证事实和明确偏好，不保存口令、密钥、人脸数据或完整聊天。未完成事项与已验证结果分开写。
多个 worktree 通过正常提交与合并同步记忆；旧 worktree 不会自动得到新提交，不覆盖其他任务的修改。
