if(RTCTRL_BUILD_CONTROL)
    add_subdirectory(realtime/posix)
    add_subdirectory(actuator/simulated)
    add_subdirectory(actuator/shared_memory)
    add_subdirectory(actuator/dynamixel)
    add_subdirectory(transport/loopback)
    if(RTCTRL_ENABLE_POSIX_SHM)
        add_subdirectory(ipc/posix_shm)
    endif()
    if(RTCTRL_ENABLE_KERNEL_MAILBOX)
        add_library(rtctrl_uapi INTERFACE)
        target_include_directories(rtctrl_uapi INTERFACE "${PROJECT_SOURCE_DIR}/include/uapi")
        add_subdirectory(actuator/mailbox)
    endif()
    if(RTCTRL_ENABLE_SERIAL)
        add_subdirectory(transport/serial)
    endif()
    if(RTCTRL_ENABLE_SOCKETCAN)
        add_subdirectory(transport/socketcan)
    endif()
    if(RTCTRL_ENABLE_IGH_ETHERCAT)
        add_subdirectory(ethercat/igh)
    endif()
endif()
if(RTCTRL_BUILD_VISION)
    add_subdirectory(capture/synthetic)
    if(RTCTRL_ENABLE_V4L2)
        add_subdirectory(capture/v4l2)
    endif()
endif()
