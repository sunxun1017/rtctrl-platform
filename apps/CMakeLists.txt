if(RTCTRL_BUILD_CONTROL)
    add_executable(rtctrl_demo rtctrl_demo.cpp)
    target_link_libraries(rtctrl_demo PRIVATE rtctrl_runtime rtctrl_bridge rtctrl_hal_sim
        rtctrl_control rtctrl_platform_posix rtctrl_source_loopback)
    add_executable(rtctrl_bench rtctrl_bench.cpp)
    target_link_libraries(rtctrl_bench PRIVATE rtctrl_timer rtctrl_platform_posix)
    add_executable(rtctrl_frame_demo rtctrl_frame_demo.cpp)
    target_link_libraries(rtctrl_frame_demo PRIVATE rtctrl_source_framed rtctrl_source_loopback rtctrl_protocol_target)
endif()
if(RTCTRL_BUILD_VISION)
    add_subdirectory(camera_capture)
endif()
if(RTCTRL_PRODUCT STREQUAL "robot-vision")
    add_executable(rtctrl_vision_control_replay rtctrl_vision_control_replay.cpp)
    target_link_libraries(rtctrl_vision_control_replay PRIVATE rtctrl_runtime rtctrl_bridge
        rtctrl_control rtctrl_hal_sim rtctrl_platform_posix rtctrl_source_loopback)
endif()

if(RTCTRL_ENABLE_FACE_RECOGNITION)
    add_subdirectory(face_recognition)
endif()
