find_package(OpenCV REQUIRED COMPONENTS core imgproc imgcodecs)
add_library(rtctrl_face_algorithms STATIC pipeline.cpp)
target_compile_features(rtctrl_face_algorithms PUBLIC cxx_std_17)
target_include_directories(rtctrl_face_algorithms PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}" ${OpenCV_INCLUDE_DIRS})
target_link_libraries(rtctrl_face_algorithms PUBLIC rtctrl_inference ${OpenCV_LIBS})
if(RTCTRL_FACE_BUILD_CLI)
  find_package(OpenSSL REQUIRED COMPONENTS Crypto)
  add_executable(rtctrl_face main.cpp digest.cpp)
  target_link_libraries(rtctrl_face PRIVATE rtctrl_face_algorithms rtctrl_inference_rknn rtctrl_rknn_runtime_loader OpenSSL::Crypto)
  if(RTCTRL_BUILD_TESTS)
    add_executable(rtctrl_face_digest_test digest_test.cpp digest.cpp)
    target_link_libraries(rtctrl_face_digest_test PRIVATE OpenSSL::Crypto)
    target_compile_features(rtctrl_face_digest_test PRIVATE cxx_std_17)
    add_test(NAME face_model_digest COMMAND rtctrl_face_digest_test)
  endif()
endif()
if(RTCTRL_BUILD_TESTS)
  add_executable(rtctrl_face_tests tests.cpp)
  target_link_libraries(rtctrl_face_tests PRIVATE rtctrl_face_algorithms)
  add_test(NAME face_algorithms COMMAND rtctrl_face_tests)
endif()

if(RTCTRL_FACE_BUILD_VIDEO)
  add_library(rtctrl_face_video_frame STATIC video_frame.cpp)
  target_link_libraries(rtctrl_face_video_frame PUBLIC rtctrl_capture ${OpenCV_LIBS})
  target_include_directories(rtctrl_face_video_frame PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}" ${OpenCV_INCLUDE_DIRS})
  target_compile_features(rtctrl_face_video_frame PUBLIC cxx_std_17)
  add_library(rtctrl_face_preview STATIC preview_server.cpp)
  target_compile_features(rtctrl_face_preview PUBLIC cxx_std_17)
  target_link_libraries(rtctrl_face_preview PUBLIC Threads::Threads rtctrl_capture_api)
  add_executable(rtctrl_face_video video_main.cpp digest.cpp)
  target_link_libraries(rtctrl_face_video PRIVATE rtctrl_face_algorithms rtctrl_face_video_frame
    rtctrl_face_preview rtctrl_inference_rknn rtctrl_rknn_runtime_loader
    rtctrl_vision_v4l2 OpenSSL::Crypto Threads::Threads)
  if(RTCTRL_BUILD_TESTS)
    add_executable(rtctrl_face_video_frame_test video_frame_test.cpp)
    target_link_libraries(rtctrl_face_video_frame_test PRIVATE rtctrl_face_video_frame)
    add_test(NAME face_video_frame COMMAND rtctrl_face_video_frame_test)
    add_executable(rtctrl_face_preview_test preview_server_test.cpp)
    target_link_libraries(rtctrl_face_preview_test PRIVATE rtctrl_face_preview)
    add_test(NAME face_preview_http COMMAND rtctrl_face_preview_test)
    set_tests_properties(face_preview_http PROPERTIES TIMEOUT 15)
    add_executable(rtctrl_face_video_match_test video_match_test.cpp)
    target_link_libraries(rtctrl_face_video_match_test PRIVATE rtctrl_face_algorithms)
    add_test(NAME face_video_empty_gallery COMMAND rtctrl_face_video_match_test)
    add_test(NAME face_video_help COMMAND rtctrl_face_video --help)
  endif()
endif()
