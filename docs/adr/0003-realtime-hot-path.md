# ADR-0003：实时热路径与安全所有权

状态：Accepted，2026-09-01。

## 决策

周期循环使用单调绝对时钟；超期直接前移到未来 deadline，禁止补跑风暴。启动完成后热路径不得动态分配、持锁、写文件日志、传播异常或无界阻塞。

HAL 的 `open_safe()` 只初始化且保持无能量输出，`arm()` 是独立显式转换；runtime 的 I/O 线程是安全状态机唯一所有者，HAL 的幂等 `emergency_stop()` 是唯一紧急硬件出口。真实系统还必须有驱动器/MCU 独立 watchdog。
